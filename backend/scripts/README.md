# Backend scripts

| Script | Purpose |
|--------|---------|
| `e2e_pipeline_test.py` | End-to-end API test (Step1→Step5). Run with backend on `http://127.0.0.1:5000`. |
| `validate_step2_excel.py` | Validate Step2 pre-extract Excel output against Step1 template. |

One-off patch/recovery scripts were removed after pipeline stabilization.
